data:
  root: data
  img_size: [512, 512]

model:
  encoder: resnet34
  encoder_weights: imagenet

train:
  seed: 42
  epochs: 25
  batch_size: 4
  num_workers: 2
  lr: 0.001
  weight_decay: 0.00001
  val_ratio: 0.2
  out_dir: models

